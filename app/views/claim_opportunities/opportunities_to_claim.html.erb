<h2>Opportunities you have posted that volunteers have claimed</h2>


<table>
  <thead>
    <tr>
      <th>Position</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @claim_opportunity.each do |claim_opportunity| %>
      <tr>
        <td><%= claim_opportunity.volunteer_opportunity.Organization %></td>
        <td><%= claim_opportunity.volunteer.name  %></td>
        
        

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if user_signed_in? %>
<%= link_to 'New Volunteer opportunity', new_volunteer_opportunity_path %>
<% end %>