<%= form_for([@volunteer_opportunity, @claim_opportunity]) do |f| %>
  <% if @claim_opportunity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@claim_opportunity.errors.count, "error") %> prohibited this claim_opportunity from being saved:</h2>

      <ul>
      <% @claim_opportunity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :please_explain_why_you_are_interested_in_this_opportunity %><br>
    <%= f.text_field :please_explain_why_you_are_interested_in_this_opportunity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
